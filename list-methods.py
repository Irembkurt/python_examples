numbers = [1, 0, 5, 16, 4, 9, 10]
letters = ['a', 'g', 's', 'b', 'y', 'a', 's']

# val = min(numbers)
# val = max(numbers)
# val = min(letters)
# val = max(letters)

# val = numbers[3:6]

# numbers[4] = 40

# numbers.append('49')      #listeye eleman eklemek için append kullandım.
# numbers.insert(3, 78)     #insert listede istenilen yere eleman ekler.

# numbers.pop(0)            #pop seçilen elemanı siler. index numarasını yazıyoruz
# numbers.remove(49)        #remove seçilen karakteri siler. karakteri yazıyoruz

# numbers.sort()            #sort ile liste sayısal büyüklük olarak sıralanır.
# letters.sort()            #sort ile liste alfabetik olarak sıralanır

# numbers.reverse()         #reverse listeyi tam tersine çevirir.

# print(len(numbers))

# print(numbers.count(10))    #count istediğimiz elemanın bilgi içinde kaç tane olduğunu sayar.

numbers.clear()
print(numbers)